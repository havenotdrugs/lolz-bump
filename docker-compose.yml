services:
  lolz-bump:
    build: .
    image: lolz-bump:latest
    container_name: lolz-bump
    env_file:
      - .env
    volumes:
      - ./data:/data
      - lolz-bump-secrets:/secrets
    restart: unless-stopped

volumes:
  lolz-bump-secrets:
